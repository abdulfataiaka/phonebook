import Header from '../Header';

const UpsertContactPage = () => {
    return (
        <div>
            <Header />
            <div>This is a page to add or edit a contact</div>
        </div>
    );
}

export default UpsertContactPage;
