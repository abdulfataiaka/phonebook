import './styles.css';

const Header = () => {
    return (
        <div className="header">
            <span>Phonebook</span>
            <button>Add</button>
        </div>
    )
}

export default Header;
